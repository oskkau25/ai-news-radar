# AI News Radar

Minimal React starter that shows a single page with 5 hard-coded AI news examples.

## Run locally

1. Install Node.js 18+.
2. Install dependencies:
   - `npm install`
3. Start dev server:
   - `npm run dev`
